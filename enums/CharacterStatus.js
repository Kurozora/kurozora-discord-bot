const CharacterStatus = Object.freeze({
    Unknown:    'Unknown',
    Alive:      'Alive',
    Deceased:   'Deceased',
    Missing:    'Missing',
})

module.exports = {
    CharacterStatus: CharacterStatus
}
