const SearchType = Object.freeze({
    Anime:      'shows',
    Manga:      'literatures',
    Characters: 'characters',
    // Episodes:   'episodes',
    // People:     'people',
    // Studios:    'studios',
})

module.exports = {
    SearchType: SearchType
}
