const SearchType = Object.freeze({
    Anime:      'shows',
    Characters: 'characters',
    // Episodes:   'episodes',
    // People:     'people',
    // Studios:    'studios',
})

module.exports = {
    SearchType: SearchType
}
